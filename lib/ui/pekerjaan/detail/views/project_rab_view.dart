import 'package:flutter/material.dart';

class ProjectRabView extends StatelessWidget {
  const ProjectRabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("RAB View"));
  }
}
