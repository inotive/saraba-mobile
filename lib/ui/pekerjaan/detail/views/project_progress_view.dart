import 'package:flutter/material.dart';

class ProjectProgressView extends StatelessWidget {
  const ProjectProgressView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Progress View"));
  }
}
